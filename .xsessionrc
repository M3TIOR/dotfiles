############################# X Session Bootstrap ########################################

### I3-wm --------------------------------------------------------------------------------
# NOTE:
#   Adding things to the $PATH here allows dmenu to access binaries an scripts within
#   those paths.

# These don't need a double check.
PATH="$HOME/.local/bin:$PATH";
 
# These do.
if [ -d "$HOME/.bin" ]; then PATH="$HOME/.bin:$PATH"; fi;
